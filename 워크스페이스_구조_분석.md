# 워크스페이스 구조 분석 (twin_minji_verPARK)

> 작성일: 2026-07-06
> 대상: `/home/dclserver78/twin_minji_verPARK`
> 목적: 전체 워크스페이스의 폴더/파일 구조, 연구 파이프라인 흐름, 작업 규칙을 한눈에 파악

---

## 1. 프로젝트 개요

이 워크스페이스는 **Sionna RT 기반 무선 채널 디지털 트윈(Digital Twin)** 연구 프로젝트입니다.
Blender 3D 씬에서 광선 추적(Ray Tracing)을 수행하고, 그 결과를 OFDM/MIMO 채널로 변환한 뒤,
빔 관리(Beam Management), 채널 공분산(CCM), 분리성(Separability), 측위(Localization),
사용자 그룹핑 등의 다양한 통신 시스템 분석으로 이어지는 **단계별 파이프라인**으로 구성됩니다.

핵심 기술 스택:
- **Sionna RT** (NVIDIA) + **TensorFlow** — 광선 추적 및 채널 생성
- **PyTorch** — 일부 학습/VAE 계열 분석
- **NumPy / SciPy** — 수치 계산
- **Blender** (`.blend`, `.ply`, `.glb`, `.obj`) — 3D 도시 지형 씬
- **Docker / devcontainer** — GPU 서버(dclcom45, dclcom55, server78) 재현 환경

---

## 2. 최상위 구조 요약

```
twin_minji_verPARK/
├── [설정/환경]
│   ├── .devcontainer/, .vscode/, .kiro/, .claude/, .git/
│   ├── Dockerfile, docker_build_2606v1.sh, requirements-*.txt
│   ├── .gitignore, .dockerignore, run_planB_venv.sh
│   └── *.log (컨테이너/도커 빌드 로그)
│
├── [작업 규칙 문서]
│   ├── AGENTS.md            ← Codex/에이전트 작업 규칙 (최우선)
│   ├── Claude.md            ← Claude 작업 규칙 (AGENTS.md와 동일 내용)
│   ├── Python_Code_Documentation.md  ← 250912~251009 코드 상세 문서
│   └── 워크스페이스_구조_분석.md      ← (본 문서)
│
├── [환경 설정 스냅샷]
│   └── 250830 서버 설정 (dclcom55)/
│
├── [연구 파이프라인 - 시간순 폴더]
│   ├── 250831 py_stable (dclcom45)/   (빈 폴더)
│   ├── 250905 py_stable (dclcom55)/   (빈 폴더)
│   ├── 250912 py_stable/              ← 기본 파이프라인 (안정판)
│   ├── 250922 python/                 ← 빔포밍 + 레이어 채널
│   ├── 250924 python/                 ← AE 레벨 + 분리성 분석
│   ├── 250925 python (P1-ABC)/        ← P1A~C 스냅샷
│   ├── 251004_Ch_Separ_(CorrRician)/  ← Kronecker 분리성 + Rician 검증
│   ├── 251009_CCM_Collection (8 GB)/  ← 대용량 Marginal CCM 수집
│   ├── 251009_Ch_Separ_Test/          ← 분리성 테스트
│   ├── 251020 BM QIE (Weichselberger)/← Weichselberger 모델 + 빔 관리
│   ├── 251028 BM_SU_MIMO_Uplink/      ← SU-MIMO 상향링크 빔 관리
│   ├── 251110 BM_SWOMP/               ← SWOMP 빔 선택
│   ├── 251218 E_MIMO_BM/              ← 확장 MIMO 빔 관리
│   ├── 260428 VAE/                    ← 기하 거리/용량 메트릭 탐색
│   ├── 260512 RT Result Data/         ← RT 결과 데이터 (GHM Twin)
│   ├── 260514 Localization/           ← UE 지문 기반 측위
│   ├── 260528 Web_Agent/              ← 웹 기반 RT 실행 에이전트 (풀스택)
│   ├── 260604 GHM_test/               ← GHM Twin 씬 테스트
│   ├── 260604 metric 도출/            ← P2 계열 채널 메트릭 도출
│   ├── 260606 test/                   ← P2G 계열 ToA/공분산 검증
│   └── 260607_temp_folder/            ← 임시 (GLB 통합 모델)
│
└── [복구 데이터]
    └── _recovery_9364ada3_260703/     ← 세션 복구 백업
```

---

## 3. 파일 네이밍 규칙

프로젝트 전반에 걸쳐 아래 패턴이 일관되게 사용됩니다 (AGENTS.md 규칙 5).

```
P{단계}{순서}_{설명}_{날짜}v{버전}.py
```

- **P1x** — 채널/레이 생성 및 통계 (RT → Rays → CCM → 분리성 → 빔 관리)
- **P2x** — 채널 메트릭 도출 및 사용자 그룹핑/클러스터링
- 예: `P1A_RT_to_Rays_2509v6.py`, `P2G_AddUECov_2606v1.py`

각 Python 파일에는 동명의 `.md`(설명)와 `.log`(진행 이력)가 동반됩니다 (규칙 1).

### P 단계 접미사 흐름 (P1 계열)

| 접미사 | 역할 |
|--------|------|
| P1A | RT → Rays (광선 추적 및 레이 추출) |
| P1B | 유효 RX 필터링 (음수 지연 제거, LoS/NLoS) |
| P1C | Rays → AE OFDM 채널 |
| P1D | AE OFDM → Gram / 분리성 분석 |
| P1E | Correlated Rician 검증 |
| P1F | Rays → Marginal CCM (R_BS, R_UE) |
| P1G | Rays → Coupling Matrix |
| P1H | Rays → Mean Channel |
| P1I | Weichselberger Chunk |
| P1J~K | Weichselberger SU-MIMO 용량 + 검증 |
| P1L~N | 빔 관리 / QIE / PADP-DFT 분석 |
| P1O | PADP → BM |
| P1P~Q | SWOMP 빔 선택 + 빔 패턴 플롯 |
| P1R~S | 기하 거리 / 용량 메트릭 (VAE 계열) |
| P1U | UE 지문 측위 |

---

## 4. 폴더별 상세

### 4.1 환경 / 설정

| 폴더/파일 | 내용 |
|-----------|------|
| `250830 서버 설정 (dclcom55)/` | GPU 서버 초기 설정 가이드, Dockerfile, vscode-server 아카이브, 설정 로그 |
| `Dockerfile`, `docker_build_2606v1.sh` | 컨테이너 빌드 정의 및 빌드 스크립트 |
| `requirements-docker.txt`, `requirements-torch.txt` | 의존성 (도커/토치 분리) |
| `Dockerfile_Setup.md/.log` | 도커 설정 문서 및 로그 |
| `.devcontainer/`, `.vscode/`, `.kiro/`, `.claude/` | IDE/에이전트 설정 |

### 4.2 연구 파이프라인 (시간순 진화)

파이프라인은 폴더명 날짜(YYMMDD)에 따라 진화합니다. **과거 스냅샷 폴더는 읽기 전용**으로 취급합니다 (AGENTS.md DO NOT 3).

- **250912 py_stable** — 기본 안정판: `P1_RT_to_Rays` → `P2_Rays_to_OFDM_Ch` → `P3_OFDM_Ch_to_ChMeanCov`. `Jonggak/`(종각) Blender 씬 포함.
- **250922 python** — 빔포밍(P2A/P2B) + 레이어 채널(P3A/P4A) 확장.
- **250924 python** — 안테나 요소(AE) 레벨 채널(P1C/P1D) + 분리성(P1E) + 경로별 빔(P2X) 추가.
- **250925 python (P1-ABC)** — P1A~C만 담은 스냅샷.
- **251004_Ch_Separ_(CorrRician)** — Kronecker 분리성(P1D) + Correlated Rician 검증(P1E). `test_*.py` 벡터화 검증 스크립트 포함.
- **251009_CCM_Collection (8 GB)** — 대용량 Marginal CCM 수집(P1F). RX별 독립 `.npz` 저장.
- **251009_Ch_Separ_Test** — 분리성 테스트 실행 환경.
- **251020 BM QIE (Weichselberger)** — Weichselberger 모델 기반 결합행렬/평균채널/용량(P1F~P1N). `docs(tex html)/` 이론 문서 포함.
- **251028 BM_SU_MIMO_Uplink** — SU-MIMO 상향링크 빔 관리(P1O). `P1O_Project/` 하위 구조.
- **251110 BM_SWOMP** — SWOMP 기반 빔 선택(P1P) + 빔 패턴 플롯(P1Q). `.tex/.pdf` 논문 자료.
- **251218 E_MIMO_BM** — 확장 MIMO 빔 관리(P1P v5), UE 분류/경로손실/빔 패턴 분석. `run_parallel.sh` 병렬 실행.
- **260428 VAE** — 기하 거리 메트릭(P1R) + 빔 용량 메트릭 탐색(P1S).
- **260514 Localization** — UE 지문 기반 측위(P1U v1/v2), 다중 RX 삼변측량 수학 명세.
- **260604 metric 도출** — P2A~P2F 채널 메트릭: 하이브리드 선형성, GHM 메트릭 비교, 사용자 그룹핑, RANTwin 보간기(FourierMLP), Bures-PDP 채널 거리, 채널 클러스터링, 각도 섹터화.
- **260606 test** — P2G 계열 ToA/공분산 검증: AddUECov, DistTerms, FitDiag, MultiBS, PairTable, PointTable, Retarget, ToA_Verify, WeightFit 등.

### 4.3 데이터 / 씬

| 폴더 | 내용 |
|------|------|
| `260512 RT Result Data/` | `channel_data_*.npz`, `Map_Mesh.obj`, RX 위치 플롯 |
| `260604 GHM_test/` | GHM Twin v0.1 씬(`.blend`, `.ply`), TX 위치 |
| `260607_temp_folder/` | `Integrated_GHMTwin_v1.0.glb` 통합 모델 |
| `_recovery_9364ada3_260703/` | 세션 복구 백업 (채널 데이터, 메타, 복구 스크립트) |

### 4.4 웹 에이전트 (풀스택)

`260528 Web_Agent/` — 웹 기반 RT 실행 에이전트로, 다른 폴더와 달리 애플리케이션 구조입니다.

```
260528 Web_Agent/
├── backend/            ← 서버 (RT 실행 API)
├── frontend/           ← UI
├── scene_library/      ← 3D 씬 라이브러리
├── scene_library_radio/← 무선 씬 라이브러리
├── scripts/            ← 유틸 스크립트
├── sessions/           ← 세션 데이터
├── logs/               ← 실행 로그
├── docs/               ← 문서
├── .venv-webagent/     ← 전용 가상환경
└── README.md, USAGE.txt
```

---

## 5. 결과물 저장 규칙

AGENTS.md 규칙 6에 따라 결과물은 단계별 하위 폴더에 저장됩니다.

```
P{단계}{순서}_{설명}_Results/
```

예시:
- `P1A_RT_Results/` — RT 광선 데이터 (`.npz`) + 메타(`.json`)
- `P1B_Valid_Results/` — 유효 RX 필터링 결과
- `P1E_Validation_Results/` — 검증 그래프(`.png`) + 수렴 로그(`.csv`)
- `P1F_Marginal_CCM_Results/` — RX별 Marginal CCM
- `P1I_Weichsel_Chunk_Results/`, `P2A_Hybrid_Metric_Results/` 등

기존 결과 파일은 **덮어쓰지 않고 타임스탬프를 붙여 새로 저장**합니다 (DO NOT 4).

---

## 6. 전체 파이프라인 흐름도

```
[Blender 3D Scene (.blend/.ply/.glb)]
        │
        ▼  Sionna RT
┌──────────────────┐
│ P1A RT → Rays    │  광선 추적, tau/power/각도 추출
└──────────────────┘
        │
        ▼
┌──────────────────┐
│ P1B Valid RX     │  음수 지연 제거, LoS/NLoS 필터
└──────────────────┘
        │
   ┌────┼─────────────┬───────────────┬──────────────┐
   ▼    ▼             ▼               ▼              ▼
┌──────┐ ┌──────────┐ ┌────────────┐ ┌───────────┐ ┌──────────┐
│P1C/D │ │ P1F CCM  │ │ P1G/H/I    │ │ P2A BF    │ │ P3A Layer│
│AE+분리│ │ Marginal │ │ Weichsel.  │ │ Pair      │ │ Channel  │
└──────┘ └──────────┘ └────────────┘ └───────────┘ └──────────┘
   │          │             │             │             │
   ▼          ▼             ▼             ▼             ▼
┌──────┐            ┌────────────┐  ┌───────────┐ ┌──────────┐
│P1E   │            │ P1J~N 용량 │  │ P2B BF    │ │ P4A Layer│
│검증  │            │ /QIE/PADP  │  │ Analysis  │ │ MeanCov  │
└──────┘            └────────────┘  └───────────┘ └──────────┘
                          │
                          ▼
                   ┌────────────┐
                   │ P1O~Q BM   │  PADP→BM, SWOMP 빔 선택
                   │ SWOMP      │
                   └────────────┘
                          │
             ┌────────────┼────────────┐
             ▼            ▼            ▼
      ┌───────────┐ ┌──────────┐ ┌──────────┐
      │ P1R/S VAE │ │ P1U 측위 │ │ P2A~G    │
      │ 메트릭    │ │ Localiz. │ │ 메트릭도출│
      └───────────┘ └──────────┘ └──────────┘
```

---

## 7. 공통 시스템 파라미터 (참고)

| 파라미터 | 대표값 | 비고 |
|----------|--------|------|
| 중심 주파수 | 7.5 GHz (기본) | 3.5 / 28 GHz 지원 |
| OFDM FFT | 128 ~ 256 | 서브캐리어 수 |
| SCS | 120 kHz | 서브캐리어 간격 |
| TX 안테나 | 8×8 패널 × 4×4 요소 (최대 1024) | UPA |
| RX 안테나 | 4×4 UPA (16) | 반파장 간격 |
| GPU | NVIDIA L40S (45GB) | dclcom45/55, server78 |

---

## 8. 작업 규칙 요약 (AGENTS.md / Claude.md)

**DO**
1. Python 파일 생성 시 동명의 `.md`(설명) + `.log`(이력) 동반
2. 로그 포맷: `날짜 - 파일 - 내용`
3. 그래프 겹침 체크 (`tight_layout()`, 범례, 폰트)
4. C 작업 시 명령어 정리 `.md` 유지
5. 네이밍 규칙 준수 (`P{단계}{순서}_{설명}_{날짜}v{버전}.py`)
6. 결과물은 `P..._Results/` 하위 폴더 저장
7. 코드 상단에 실행 환경(Python/서버/GPU/라이브러리 버전) 명시
8. 대용량 데이터(`.npz/.mat/.blend/.tar.gz`) git 제외 + 경로만 문서화
9. 시뮬레이션 파라미터 변경 시 인라인 변경 사유 주석

**DO NOT**
1. 참고용 Python 파일 임의 수정 금지 (복사 후 허락받고 수정)
2. 실패 코드 기반 수정 금지 (성공 상황만 참고)
3. 과거 스냅샷 폴더(`250912`, `250922` 등) 직접 수정 금지 (읽기 전용)
4. 결과 파일 덮어쓰기 금지 (타임스탬프 붙여 신규 저장)

---

## 9. 참고 문서

- `Python_Code_Documentation.md` — 250912~251009 폴더 코드의 목적/입출력/기능 상세
- `250912 py_stable/Sionna_RT_Pipeline_Overview.md` — RT 파이프라인 개요
- `260514 Localization/Multi_RX_Trilateration_Math_Spec_2605v1.md` — 측위 수학 명세
- `260528 Web_Agent/README.md` — 웹 에이전트 사용법
- 각 폴더의 `P*.md` — 개별 스크립트 설명서
