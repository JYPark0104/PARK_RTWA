# Claude 작업 규칙

## ✅ 해야 할 것 (DO)

1. **Python 파일 생성 시 문서화**
   - 해당 파일에 대한 설명이 담긴 `.md` 파일을 함께 생성
   - 수정 및 진행 상황을 담고 있는 `.log` 파일을 함께 생성

2. **Log 파일 포맷**
   - `날짜` - `수정 및 진행 파일` - `해당 수정 및 진행상황 내용`
   - 예시: `2026-05-07 - P1S_BM_Capacity_Metric_Search.py - 초기 버전 생성`

3. **결과 산출 코드의 그래프 체킹**
   - 그래프가 서로 겹쳐 글자가 겹치지 않도록 한번 더 체킹
   - `tight_layout()`, 범례 위치, 폰트 크기 등 확인

4. **C언어 작업 시 터미널 명령어 정리**
   - 터미널 관련 용어/명령어를 정리하는 별도 `.md` 파일 생성
   - 새로운 명령어가 사용될 때마다 계속 업데이트

5. **파일 네이밍 규칙 준수**
   - 패턴: `P{단계}{순서}_{설명}_{날짜}v{버전}.py`
   - 예시: `P1A_RT_to_Rays_2509v6.py`
   - 새 파일 생성 시 기존 네이밍 패턴과 일관성 유지

6. **결과 파일 저장 경로 규칙**
   - 결과물(`.npz`, `.png`, `.csv` 등)은 해당 단계의 하위 폴더에 저장
   - 폴더 네이밍: `P{단계}{순서}_{설명}_Results/`
   - 예시: `P1A_RT_Results/`, `P1E_Validation_Results/`

7. **코드 상단에 실행 환경 명시**
   - 파일 상단 주석에 아래 정보 기록:
     - Python 버전
     - 실행 서버 (예: dclcom55, dclcom45)
     - GPU 정보 (필요 시)
     - 주요 라이브러리 버전 (예: sionna, tensorflow, numpy)

8. **대용량 데이터 파일 관리**
   - `.npz`, `.mat`, `.blend`, `.tar.gz` 등은 git 추적 제외
   - `.gitignore`에 추가하고, 파일 경로와 설명만 문서화

9. **파라미터 변경 시 변경 사유 주석**
   - 시뮬레이션 파라미터(안테나 수, 주파수, RX 수 등) 변경 시
   - 인라인 주석으로 변경 사유를 반드시 남기기
   - 예시: `num_rx = 64  # 32 → 64로 변경: 해상도 향상 테스트 (2026-05-07)`

---

## ❌ 하지 말아야 할 것 (DO NOT)

1. **참고용 Python 파일 임의 수정 금지**
   - 만드는 Python 파일만 수정 진행
   - 참고하는 Python 파일 수정이 필요한 경우:
     1. 먼저 복사본을 생성
     2. 사용자에게 허락을 받은 후 수정 진행

2. **실패 코드 기반 수정 금지**
   - 코드가 잘 안 돌 때, 이전 실패 상황의 코드를 기반으로 수정하지 말 것
   - 성공했을 때의 상황만 참고해서 수정 진행

3. **이전 버전 폴더 내 파일 직접 수정 금지**
   - `250912 py_stable`, `250922 python` 등 과거 스냅샷 폴더는 읽기 전용으로 취급
   - 수정이 필요하면 새 폴더/파일로 복사 후 작업

4. **결과 파일 덮어쓰기 금지**
   - 기존 결과 `.npz`, `.png`, `.csv` 파일을 덮어쓰지 말 것
   - 타임스탬프를 붙여서 새로 저장
   - 예시: `P1E_fig1_covariance_errors_20260507.png`
